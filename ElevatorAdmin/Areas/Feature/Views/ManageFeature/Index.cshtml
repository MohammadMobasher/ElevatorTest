@using DataLayer.DTO;
@using DNTPersianUtils.Core;
@model List<FeatureDTO>
@{
    ViewData["Title"] = "لیست ویژگی‌ها";

}

<div class="col-lg-12 col-xl-12 col-md-12 col-sm-12">

    @*<button data-role-href="/Feature/ManageFeature/Insert"
            class="btn btn-lg btn-primary data-role-table-btn" data-toggle="tooltip" modal-title="ثبت جدید" ismodal="true" title="ثبت" data-role="confirm">
        <i class="fa fa-plus btn-icon" aria-hidden="true"></i>&nbsp;&nbsp;

        ثبت
    </button>*@
    <TableBotton
        area="Feature" controller="ManageFeature" action="Insert"
        isModal="true"
        modalTitle="ثبت جدید"
        title="ثبت"
        icon="fa-plus"
        buttonClass="btn-primary"
        ></TableBotton>
    

    <span class="hidden" data-role-table-btn-area-access="">

        <button data-role-href="/Feature/ManageFeature/Update" class="btn btn-lg btn-warning data-role-table-btn" data-toggle="tooltip" title="ویرایش" data-role="confirm">
            <i class="fa fa-pencil-square-o" aria-hidden="true"></i>&nbsp;&nbsp;
            ویرایش
        </button>

        <button data-role-href="/Feature/ManageFeature/Delete" class="btn btn-lg btn-danger data-role-table-btn" data-toggle="tooltip" title="حذف" data-role="confirm">
            <i class="fa fa-trash-o btn-icon" aria-hidden="true"></i>&nbsp;&nbsp;
            حذف
        </button>
    </span>

</div>


<div class="col-lg-12 col-xl-12 col-md-12 col-sm-12">
    @*<i class="fa fa-calendar" aria-hidden="true"></i>

        <i class="fa fa-bath" aria-hidden="true"></i>*@
    <table class="table table-responsive table-striped rounded table-hover table-bordered ">
        <thead>

            <tr>
                <th width="3%"></th>
                <th width="50px">
                    ردیف
                </th>
                <th>نام</th>
                <th width="15%">نوع فیلد</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <form method="post">
                    <td>---</td>
                    <td>---</td>
                    <td><input type="text" value="" name="Title" class="form-control searchInput" /></td>
                    <td>
                        <select name="type" asp-items="@Html.GetEnumSelectList<DataLayer.SSOT.FeatureTypeSSOT>()" required class="form-control">
                            <option value="">انتخاب نمایید...</option>
                        </select>
                    </td>
                    <input type="submit" class="hidden searchBtn" value="جستجو" />
                </form>
            </tr>


            @{var index = 1;}
            @if (Model != null && Model.Count != 0)
            {


            }
            else
            {
                <tr>
                    <td colspan="6" class="text-danger">رکوردی یافت نشد</td>
                </tr>
            }
        </tbody>
    </table>

    @Html.Partial("~/Views/Shared/_Pagination.cshtml")



</div>
